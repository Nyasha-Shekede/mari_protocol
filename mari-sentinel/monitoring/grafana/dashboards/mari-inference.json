{
  "annotations": {"list": []},
  "editable": true,
  "panels": [
    {
      "type": "stat",
      "title": "Requests Total",
      "targets": [{"expr": "sum(inference_requests_total)", "refId": "A"}],
      "gridPos": {"x": 0, "y": 0, "w": 6, "h": 4}
    },
    {
      "type": "graph",
      "title": "Latency (s)",
      "targets": [{"expr": "histogram_quantile(0.95, sum(rate(inference_duration_seconds_bucket[5m])) by (le))", "refId": "A"}],
      "gridPos": {"x": 6, "y": 0, "w": 12, "h": 8}
    },
    {
      "type": "graph",
      "title": "Score Distribution",
      "targets": [{"expr": "sum(rate(inference_score_bucket[5m])) by (le)", "refId": "A"}],
      "gridPos": {"x": 0, "y": 4, "w": 12, "h": 8}
    },
    {
      "type": "table",
      "title": "Model Version",
      "targets": [{"expr": "model_version_info", "refId": "A"}],
      "gridPos": {"x": 12, "y": 8, "w": 6, "h": 4}
    }
  ],
  "schemaVersion": 38,
  "version": 1,
  "title": "Mari Inference"
}
